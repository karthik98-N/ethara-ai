{
  "name": "flowforge",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered collaborative task management platform.",
  "scripts": {
    "dev": "concurrently \"npm.cmd --prefix server run dev\" \"npm.cmd --prefix client run dev\"",
    "install:all": "npm.cmd install && npm.cmd --prefix client install && npm.cmd --prefix server install",
    "build": "npm.cmd --prefix client run build",
    "start": "npm.cmd --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
